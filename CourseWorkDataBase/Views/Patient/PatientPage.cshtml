@using CourseWorkDataBase.Models
@model IEnumerable<Doctor>

@{
    ViewData["Title"] = "Список Врачей";
}

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"] - Медицинский Центр</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-5">
    <h1 class="mb-4">Наши Врачи</h1>

    <table class="table table-striped">
        <thead>
        <tr>
            <th>ФИО</th>
            <th>Специализация</th>
            <th>Опыт (лет)</th>
            <th>Действия</th>
        </tr>
        </thead>
        <tbody>
        @if (Model != null && Model.Any())
        {
            foreach (var doctor in Model)
            {
                <tr>
                    <td>@doctor.FirstName</td>
                    <td>@doctor.FamilyName</td>
                    <td>@doctor.Specialty.NameSpecialty</td>
                    <td>
                        <a href="@Url.Action("ViewDoctor", "Patient", new { doctorId = doctor.ID })" class="btn btn-info btn-sm">Просмотр</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="4" class="text-center">Врачи не найдены.</td>
            </tr>
        }
        </tbody>
    </table>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>